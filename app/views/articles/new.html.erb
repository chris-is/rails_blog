<!--Note that plularize is a rails helper that takes a number and a string as its arguments and if the number is > 1, the string will be automatically be "pluralized".-->
<%= render 'form' %>
 
<%= link_to 'Back', articles_path %>

